## 一、AI 交互日志

按模块（M1~M4）记录我使用 ChatGPT（或其它 AI）辅助编程的关键过程。

### M1 数据处理模块

- **Prompt 示例**（2025-05-10）：  
  “我有一份 NYC 出租车 2023年1月的数据，字段包括 tpep_pickup_datetime, tpep_dropoff_datetime, trip_distance, fare_amount, PULocationID 等。请帮我写一个 Python 函数生成数据质量报告，包括缺失率和异常值（距离<=0，车费<=0）。”

- **AI 输出摘要**：  
  给出了一个 quality_report() 函数，使用 `df.isnull().sum()/len(df)*100` 计算缺失率；对距离和车费用布尔索引统计异常值，并用 print 输出。

- **我的决策**：**采用**，但增加了对 passenger_count 异常值的检查，并加入了“车费>500 也视为异常”的逻辑。  
- **AI 犯错案例**：  
  AI 最初只检查了 trip_distance <=0 和 fare_amount <=0，没有考虑极端大值（如距离>100英里、车费>500美元）。  
  我在运行报告后发现有些行程距离显示为 0.1 英里但车费高达 800 美元，意识到原始数据中存在极端离群点，于是手动补充过滤条件 `(df['trip_distance'] < 100)` 和 `(df['fare_amount'] < 500)`，并在清洗模块中体现。

### M2 可视化模块

- **Prompt**：  
  “请帮我实现一个函数，绘制分小时平均订单量的折线图，并且区分工作日和周末。使用 seaborn 或 matplotlib。”

- **AI 输出**：  
  给出了一段代码，使用 `groupby(['pickup_hour', df['pickup_weekday'].apply(lambda x: 'weekend' if x>=5 else 'weekday')])`。  
- **决策**：**修改后采用**。  
  我发现 AI 直接对每个小时分组累计订单量，没有考虑不同日期天数不一致（周末只有8天，工作日有23天），导致周末的累计订单量看起来很低。于是我改成了“先按日期+小时聚合，再按小时和周末求平均”的逻辑，这样对比更公平。

### M3 预测模型模块

- **Prompt**：  
  “我有每小时某区域的需求量数据（demand），特征包括小时、星期、是否高峰。请用 TensorFlow 构建一个简单的回归神经网络，并与随机森林对比。”

- **AI 输出摘要**：  
  给出了一个 `Sequential` 模型（64-32-1），编译时使用 `'mse'` loss 和 `'mae'` metrics，训练 50 epochs；同时提供了随机森林的训练代码和评价指标计算。  
- **决策**：**采用**，但补充了 loss 曲线的保存和对比结果的文本输出。  
- **AI 犯错案例**：  
  AI 没有对特征进行标准化，导致神经网络收敛很慢。我发现 loss 曲线下降非常平缓，检查后发现特征“pickup_hour”（0~23）范围与其他特征差别较大。于是添加了 `StandardScaler` 对 X 进行标准化，重新训练后 loss 正常下降。

### M4 问答接口模块

- **Prompt**：  
  “请给我一个命令行交互循环，支持用户自然语言提问，能识别‘区域排名’、‘费用估算’、‘需求预测’三种问题。”

- **AI 输出**：  
  给出了一个基于 `if '上客量' in question.lower()` 的简单规则匹配系统，每个分支调用提前写好的函数。  
- **决策**：**采用**，但扩展了问题类型到 5 种（增加了“繁忙时段”、“平均速度”）。  
- **AI 犯错案例**：  
  AI 在费用估算中使用了固定每英里单价（2.5美元），但实际数据中车费与距离并非完全线性，且存在起步价、拥堵附加费等。我意识到这里需要从数据中动态计算中位数单价，于是修改为 `avg_fare_per_mile = (df['fare_amount'] / df['trip_distance']).median()`，使估算更准确。

---

## 二、三阶段对比 – 以“异常值过滤”为例

我选择 **M1 中的异常值过滤函数**作为典型功能，对比三种开发模式。

### 1. Native 版（独立写，无 AI）

```python
def clean_data_native(df):
    # 删除缺失关键字段的行
    df = df.dropna(subset=['tpep_pickup_datetime', 'trip_distance', 'fare_amount'])
    # 过滤距离和车费
    df = df[df['trip_distance'] > 0]
    df = df[df['fare_amount'] > 0]
    # 过滤乘客人数
    if 'passenger_count' in df.columns:
        df = df[df['passenger_count'] > 0]
    return df
```

**效率**：花了约 20 分钟思考哪些字段需要过滤、写代码、测试。  
**理解深度**：完全自己控制逻辑，但容易遗漏阈值（如未考虑上限）和业务规则。

### 2. Prompt 版

**Prompt**：“写一个清洗出租车数据的函数，删除 trip_distance <=0 或 fare_amount <=0 的行，并删除缺失 pickup/dropoff 时间的行。”

```python
def clean_data_prompt(df):
    df = df.dropna(subset=['tpep_pickup_datetime', 'tpep_dropoff_datetime'])
    df = df[(df['trip_distance'] > 0) & (df['fare_amount'] > 0)]
    return df
```

**效率**：30 秒得到代码，无需查语法。  
**理解深度**：低 – 我不需要知道具体怎么写，但必须自己发现 AI 没处理 passenger_count 异常、没过滤极端大值。如果盲目信任，就会埋下隐患。

### 3. Vibe 版（多轮对话驱动）

**对话摘要**：  
- 我：“过滤异常距离和车费。”  
- AI：“已添加 `df[df['trip_distance'] > 0]`。”  
- 我：“如果距离超过 100 英里呢？”  
- AI：“建议加入 `df[df['trip_distance'] < 100]`。”  
- 我：“车费有没有上限？”  
- AI：“根据 NYC 出租车数据，正常车费很少超过 500 美元，可以加上 `fare_amount < 500`。”  
- 我：“乘客人数可能有 0 吗？”  
- AI：“有极少数为 0 的记录，应保留 1-6 人。”

最终代码：

```python
def clean_data_vibe(df):
    df = df.dropna(subset=['tpep_pickup_datetime', 'tpep_dropoff_datetime', 'trip_distance', 'fare_amount'])
    df = df[(df['trip_distance'] > 0) & (df['trip_distance'] < 100)]
    df = df[(df['fare_amount'] > 0) & (df['fare_amount'] < 500)]
    if 'passenger_count' in df.columns:
        df = df[(df['passenger_count'] >= 1) & (df['passenger_count'] <= 6)]
    return df
```

**效率**：总共 5 分钟对话 + 复制粘贴，比 Native 快但比单次 Prompt 慢。  
**理解深度**：中等 – 通过对话我逐步明确了业务规则（距离上限、车费上限、人数范围），比直接抄代码理解更深，而且 AI 帮助我发现了自己没想到的边界情况（如乘客为 0）。

**总结**：  
- Native 适合自己非常熟悉的小函数；  
- Prompt 适合快速原型，但必须严格测试；  
- Vibe 适合需要探索业务边界、逐步求精的场景。

---

## 三、反思 – AI 工具的能力边界

完成本次作业后，我对 AI 编程助手的能力边界有了以下新认识：

1. **AI 擅长语法和常见模式，但不理解业务语义**。  
   它能迅速写出 `groupby`、`pandas` 清洗代码，但不会主动判断“出租车行程距离超过 100 英里是否合理”、“乘客人数为 0 是否要删除”。这些业务知识需要我明确告知或自己补充。

2. **AI 输出的代码通常“看起来合理”，但可能隐藏逻辑错误**。  
   比如在 M3 中，AI 没有对特征做标准化就训练神经网络，导致收敛极慢；在 M2 中，AI 画的“工作日 vs 周末”订单量对比图没有考虑天数差异，导致视觉误导。**我不能直接信任它的输出，必须用测试数据验证**。

3. **AI 无法进行端到端的项目设计**。  
   它能给出孤立函数，但不会主动提示“如果你的数据有 300 万行，内存不足怎么办”、“问答系统应该先预测还是先加载模型”。这些工程决策仍然需要我来做。

4. **AI 能显著提升效率，但前提是我要能提出正确的问题**。  
   例如，如果我不知道“标准化”这个概念，就不会发现神经网络训练的问题；如果我不了解 NYC 出租车业务，就不会主动要求过滤极端大值。**使用 AI 的瓶颈不再是写代码，而是我的领域知识和系统思维**。

因此，我的结论是：AI 目前更像一个“知识面广但缺乏判断力的初级程序员”，我可以让它承担机械性编码工作，但设计、审查、集成和调试仍然必须由我主导。

---

